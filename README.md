## Important Commands

```
npm run dev  # run in development mode
npm run build  # build the static page
npx serve out  # serve the built app
npm run deploy  # deploy the website on the server
npm run build-deploy  # build and deploy at the same time
```
